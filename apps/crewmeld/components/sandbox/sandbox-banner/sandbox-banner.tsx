'use client'

/** Sandbox banner — placeholder until sandbox subsystem is fully ported. */
export function SandboxBanner() {
  return (
    <div className='flex items-center justify-center rounded-t-lg border-amber-200 border-b bg-amber-50 px-4 py-1.5 text-amber-700 text-xs'>
      Sandbox mode active
    </div>
  )
}
