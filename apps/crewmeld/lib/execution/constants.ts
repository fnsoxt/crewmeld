/**
 * Execution constants stub — P0 exposes only the default timeout.
 *
 * TODO: P1 port real implementation from upstream engine (lib/execution/constants.ts).
 */

export const DEFAULT_EXECUTION_TIMEOUT_MS = 30_000
