/**
 * Channel plugin entry point — registers all built-in channel plugins.
 *
 * This is a side-effect import: import '@/lib/channels/plugins'
 * Individual channel plugins (Feishu, WeCom, DingTalk) will be registered
 * here as they are ported in later waves.
 */

// Channel plugins registered here (ported incrementally in Wave 5)
