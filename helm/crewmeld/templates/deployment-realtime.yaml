{{- if .Values.realtime.enabled }}
{{- include "crewmeld.validateSecrets" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "crewmeld.fullname" . }}-realtime
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "crewmeld.realtime.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.realtime.replicaCount }}
  selector:
    matchLabels:
      {{- include "crewmeld.realtime.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "crewmeld.realtime.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "crewmeld.serviceAccountName" . }}
      {{- include "crewmeld.podSecurityContext" .Values.realtime | nindent 6 }}
      {{- include "crewmeld.nodeSelector" .Values.realtime | nindent 6 }}
      {{- include "crewmeld.tolerations" .Values | nindent 6 }}
      {{- include "crewmeld.affinity" .Values | nindent 6 }}
      containers:
        - name: realtime
          image: {{ include "crewmeld.image" (dict "context" . "image" .Values.realtime.image) }}
          imagePullPolicy: {{ .Values.realtime.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.realtime.service.targetPort }}
              protocol: TCP
          env:
            - name: DATABASE_URL
              value: {{ include "crewmeld.databaseUrl" . | quote }}
            {{- if .Values.app.env.REDIS_URL }}
            - name: REDIS_URL
              value: {{ .Values.app.env.REDIS_URL | quote }}
            {{- end }}
            {{- range $key, $value := .Values.realtime.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.telemetry.enabled }}
            # OpenTelemetry configuration
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://{{ include "crewmeld.fullname" . }}-otel-collector:4318"
            - name: OTEL_SERVICE_NAME
              value: crewmeld-realtime
            - name: OTEL_SERVICE_VERSION
              value: {{ .Chart.AppVersion | quote }}
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name=crewmeld-realtime,service.version={{ .Chart.AppVersion }},deployment.environment={{ .Values.realtime.env.NODE_ENV }}"
            {{- end }}
            {{- with .Values.extraEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          envFrom:
            # App secrets (authentication keys shared with main app)
            - secretRef:
                name: {{ include "crewmeld.appSecretName" . }}
            # Database secrets
            {{- if .Values.postgresql.enabled }}
            - secretRef:
                name: {{ include "crewmeld.postgresqlSecretName" . }}
            {{- else if .Values.externalDatabase.enabled }}
            - secretRef:
                name: {{ include "crewmeld.externalDbSecretName" . }}
            {{- end }}
          {{- if .Values.realtime.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.realtime.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.realtime.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.realtime.readinessProbe | nindent 12 }}
          {{- end }}
          {{- include "crewmeld.resources" .Values.realtime | nindent 10 }}
          {{- include "crewmeld.securityContext" .Values.realtime | nindent 10 }}
          {{- if or .Values.extraVolumeMounts .Values.realtime.extraVolumeMounts }}
          volumeMounts:
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- with .Values.realtime.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
      {{- if or .Values.extraVolumes .Values.realtime.extraVolumes }}
      volumes:
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.realtime.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
{{- end }}